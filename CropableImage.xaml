<UserControl x:Class="WpfCropableImageControl.CropableImage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="{Binding ActualCropHeight}" Width="{Binding ActualCropWidth}" SnapsToDevicePixels="True" UseLayoutRounding="True" >

    <StackPanel Width="{Binding BigImageWidth}">
        <Grid x:Name="MainGrid"/>
    </StackPanel>

</UserControl>